# 项目初审

初审日期：2026-09-12。下文保留接管时的资料审计；当前已完成首版本地实现，实际状态以 FINAL_AUDIT.md、TASKS.md 为准。

## 初审时的真实状态

上游 `main` 的提交 `7e81ed2d05517be7fc66d9a960bcaaf210bc7b45` 仅含 LICENSE 和 `预测项目.zip`。压缩包内共 21 份资料：12 份 Markdown、5 份 DOCX、2 份 PDF、1 份 TXT、1 份旧版 DOC。两份 PDF 分别为 23、28 页。

没有既有后端、前端、数据库迁移、依赖锁文件、测试、Docker 服务或 GitHub Actions，也没有实际历史开奖数据。文档中的代码块和演示数值不是已运行的工程或实验结果。

## 本次检查范围与证据

- 完整读取用户引用的四轮聊天，区分通用自主工作方式与 LottoLab 业务要求。
- 通过 GitHub 连接读取仓库、默认分支、文件列表、提交和树对象；本机聊天附件与 GitHub 压缩包的 Git blob 校验值一致。
- 21 份资料全部提取为可检索文本，并逐份对目标、架构及方法要求作初审；清单记录原文件大小、SHA-256、文本长度和提取方式。
- 保留全部原始资料，未执行其中代码、Office 宏或命令。DOC 使用二进制文本表读取，DOCX 读取 XML，PDF 提取文本。
- 本次是需求与设计初审，不是所有示例代码、外部引用或数据接口的完整技术验证；后续实现相关模块时还须阅读对应完整章节并验证。
- `muse-spark-1.3` 的原始 Markdown 本身含 15 个替换字符，原样保留并标记；不能凭猜测修复原作者意图。
- 本地 Git 的 6 个对象均与上游标识吻合，两次提交历史完整，`git fsck --full --no-reflogs` 通过。原有 LICENSE 与压缩包无差异。

## 主要冲突与处理

详细依据见 [方案评审](docs/DESIGN_REVIEW.md)。

1. 产品名称、首批彩种和页面清单不一致：统一为 LottoLab，先 SSQ 完整流程，再扩 DLT。
2. 技术栈混合 Vue、React、Next.js、Streamlit、Cloudflare D1 及多个数据库：采用独立 Python 服务、PostgreSQL 与 React 页面，减少运行体系。
3. 一些方案要求模型必须打赢随机，另一些要求必须不显著：两种预设都取消，验收方法和可复现性。
4. 部分概率、收益假设和表格有误：组合概率由公式计算；奖金按版本与历史来源计算；示例数字不直接成为常量或测试预期。
5. 同期开奖内无放回依赖、多标签概率、未来特征和公众选号数据被混用：写入项目规则与阶段验收，阻止产生误导性结果。
6. “七天全部上线”缺乏环境和人员前提：改成以真实退出条件驱动的阶段，不承诺无证据的工期。

## 风险与边界

- 数据：来源网址、最新游戏规则、历史覆盖和节假日开奖日历尚未在线验证。缺失数据必须明确标识，不用合成数据补齐真实记录。
- 统计：需要正确零模型、多重比较、时间切分和防泄漏测试。p 值不能独立承担随机性证明、预测证明或产品宣传。
- 工程：只有设计，没有可运行服务；不能把配置完成标记为平台完成。
- 安全：未来数据库写入、批量任务和对外接口需参数验证、资源上限与适当鉴权，凭据不得进入 Git。
- 环境：初次处理时终端网络与全局文件写入受限，自动审批返回 `codex-auto-review` 模型不受支持（404）。后续会话明确开放权限后，官网文档已成功读取；自动审查接口本身未复测。
- Codex 全局规则：已在后续获准的会话中安装并核对 SHA-256，与项目内 AGENTS.md 共同提供规则。原全局空文件已备份，安装期间没有修改模型、连接或权限配置。

## 推荐实现与验收

规格见 PROJECT_SPEC.md；按 PLAN.md、TASKS.md 推进。优先交付合法数据导入、数据库、基础查询与页面的一条完整流程，再实现统计、无泄漏回测、模型、模拟和覆盖优化。

后续完成声明必须有真实执行证据。当前业务测试、前端构建、数据库迁移、容器启动和线上 CI 均为 SKIPPED：相应工程尚未建立。

## 2026-09-12 开发与交付更新

已建立 FastAPI、React、SQLAlchemy/Alembic、SQLite/PostgreSQL、独立计算进程、55 项后端测试、6 条浏览器验收、容器和 CI 配置。八个页面及 SSQ/DLT 数据与实验已可本地使用，详见 README 与 FINAL_AUDIT。

当前本机 SQLite 收录官方快照恢复的 SSQ 1,000 期和 DLT 300 期，导入无拒绝或冲突；真实科学结果写入 SCIENTIFIC_AUDIT。本地启动/停止/重启、数据持久化和备份通过。

Docker Desktop 在恢复会话中因旧通信文件失败，清理操作被自动审批拒绝；最终容器复验受阻。PostgreSQL 原卷保留。CI 已配置但未远程运行，未公开部署。初审中的“业务未实现”与 SKIPPED 描述仅代表接管时点。

## 免费云扩展审计

用户随后选择“电脑关机后完整使用，优先免费云服务”。方案采用 Vercel Hobby + Neon Free，保持既有 Python 算法；用请求内限时子进程替代云端常驻 worker，压缩快照与所有状态存 PostgreSQL。Cloudflare DNS 可选，不启用付费容器或无必要的对象存储。

云改造最终 88 项后端测试、原有 7 条本地浏览器验收、真实 PostgreSQL 17.10、迁移往返及并发限制通过。迁移检查发现非 UTC 服务器可能重解释 SQLite 的无时区时间，已修复并以往返测试验证。1,300 期及所有历史实验/快照迁入真实 Neon 后指纹相符，来源未写入。

2026-09-13 已完成 Vercel Hobby 正式发布：https://lottolab-zeta.vercel.app。用户接受条款后创建正式/预览两个独立 Neon free_v3 数据库，均核实为 Free、不要求信用卡。Vercel Linux Python 3.12.14 真实计算通过；预览/正式均完成八页面、浏览器计算、刷新恢复和 390px 布局检查。默认 120 期四模型回测为 17.22 秒。

实际云验收暴露并修复了源码导入路径、子进程重新加载平台启动程序和未安装本项目 distribution 的版本记录问题。回归覆盖不允许重新导入的宿主入口、未安装的源码、超时与取消。原有并发和持久化边界保持。当前部署未见运行错误；历史失败任务留在独立预览库中。

iad1 的官方数据接口暂不可用，两个彩种均通过备用来源成功同步，导入报告记录来源与官方失败提示。当前为手动触发同步，没有定时后台自动更新。私有令牌、连接串和浏览器凭据均排除在 Git 与部署上传之外。具体配额、凭据位置、验证证据与后续维护见 TASKS.md、docs/CLOUD_DEPLOYMENT.md 和 docs/validation/2026-09-13-cloud.json。
