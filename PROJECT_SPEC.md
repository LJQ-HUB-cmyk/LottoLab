# LottoLab 产品与工程规格

版本：1.0.0 封板验收范围，2026-09-13。用户已选择电脑关机后仍可使用、优先免费云服务。Vercel Hobby + Neon Free 已完成正式部署，实际验证状态见 FINAL_AUDIT.md 和 TASKS.md。初始方案取舍见 docs/IMPLEMENTATION_NOTES.md。

## 产品目标

为历史开奖数据提供可靠采集、描述统计、随机性研究、概率模型实验、严格时间回测、Monte Carlo 与组合覆盖分析。用户能追溯数据来源和实验设置，理解结果及不确定性，并复现实验。

产品不包含售彩、代投注、资金账户、付费荐号、中奖承诺或根据未经验证的“规律”提供盈利建议。号码生成作为标记清楚的研究/娱乐样本。

## 范围与技术方向

首版支持 SSQ：主区 33 选 6，附加区 16 选 1；支持 DLT：主区 35 选 5，附加区 12 选 2。号码空间规则带版本号，基础单注票价为 2 元。SSQ 收益依赖历史每注奖级奖金；DLT 历史奖级生效区间尚未完整建模，首版禁用 DLT ROI。

采用 Python 3.12、FastAPI、Pydantic、SQLAlchemy 与 Alembic；持久化采用受支持版本的 PostgreSQL。统计计算使用 NumPy、SciPy、scikit-learn；树模型优先选择兼容性良好的现成实现。实际依赖版本在创建工程时验证并锁定。

前端采用 React、TypeScript、Vite 与成熟图表库。独立 API 和交互仪表盘适合静态页面部署，首期没有服务端页面渲染需求。前端编译后的静态文件由 API 提供。Docker Compose 组织数据库、API、worker；CPU 密集实验采用独立子进程及持久任务状态。本机默认 SQLite，可选择 PostgreSQL，使用同一模型与迁移。

首期模块保持单仓库、清晰边界。大数据服务、消息集群、额外缓存、复杂序列模型及社交功能均按明确需求再评估。

免费云扩展采用 Vercel Hobby 的 FastAPI/静态页面和外部 Neon Free PostgreSQL。单请求监管一个具有独立 Python 入口的子进程，默认最多 240 秒；通过私有标准输入传递配置和运行时依赖路径，避免重新执行托管平台启动程序。数据库层限制同时一个活动任务，无需常驻 worker。原始快照压缩入库，使用短连接，私人 API 读写均需令牌。免费额度不代表无限计算，超时必须保存失败状态并允许重试。Cloudflare 仅作为可选的域名 DNS；不要求本机、Docker 或付费容器保持运行。

## 数据契约

流程：来源适配器 → 原始快照 → 解析 → 校验 → 规范化与修订 → 数据版本 → 统计/特征/实验。

优先官方公开来源，支持第二来源交叉检查和 CSV 手工导入。网络来源要有超时、有限重试、退避、限速、状态和错误记录；来源不可用时历史查询与 CSV 导入仍可使用。

持久对象为 ingestion_runs、draws、quality_issues、dataset_versions、jobs、raw_snapshots。号码数组保存在带约束的 JSON 字段，规则与模型目录在代码中版本化；参数、逐期结果和优化结果保存在任务及冻结数据中。原始快照在本地采用文件存储，在云端采用按 SHA-256 去重且可还原校验的数据库存储。

关键约束：

- 期号按字符串保存，唯一键为 `(lottery_code, issue)`；实际日期合法并符合对应规则与日历。
- 主区与附加区分别校验数量、范围、整数性和去重，规范化排序不丢弃原始顺序来源。
- 同一期多来源冲突须留痕、隔离或产生明确修订，重导入幂等；禁止静默覆盖真实结果。
- 原始快照保存 source_url、导入时间与 SHA-256；来源抓取时间与本地恢复时间不混淆。开奖及冻结数据保留 ingestion_id。任务保存执行代码指纹；首版没有独立解析器版本表或完整的历史发布时点档案。
- 销售额、奖池和派奖允许缺失且不插值；金额使用 Decimal/整数最小单位。规则版本与数据版本参与实验标识。
- 真实、合成与演示数据显式区分，页面和导出保留标记。

## 统计与零模型

实现频率、遗漏、和值、跨度、奇偶、大小、分区、连号、重号和共现统计，并注明定义、窗口和样本量。

在独立、均匀、规则不变的模型下：SSQ 整注空间为 17,721,088；DLT 整注空间为 21,425,712。SSQ 每号红球入选概率为 6/33；预测六个红球的命中数服从 Hypergeometric(33,6,6)，期望为 36/33。数值表从公式生成。

随机性研究包括适用的频数检验、游程、ACF/Ljung-Box，以及正确零模型下的 Monte Carlo 检验。奇偶/区间计数、和值、排序位置分别使用对应的无放回分布；离散数据不能直接套连续 KS 的默认 p 值。一次开奖作为整体抽样单元。

记录统计量、检验假设、原始 p 值、适用的效应量及置信区间、检验总数、检验族和 Bonferroni/BH 等校正方法；相关性不满足方法前提时采用合适的更稳健方案。验收不规定显著性的方向。

## 特征、模型与回测

描述性开奖特征与预测特征分离。每个预测特征有 `as_of`、窗口、版本和可用时间；第 t 期预测只能使用开奖前可得的信息。需要拟合的预处理只在训练区间拟合。首版采用固定超参数，不提供自动调参或独立概率校准模型；基数投影不等同于校准。

按期进行有上限训练窗口的 walk-forward；同一期号码级训练行不能分散到训练和测试两侧。本次运行的末段测试区间、主要指标与比较族固定。首版允许重复探索，未建立不可再访问的独立确认性检验区；确认性试验需要另行预登记。

模型顺序：Uniform、Historical Frequency、Logistic Regression、Gradient Boosting；额外模型作为可选实验。数据接口、预测接口和随机数生成器统一，种子可控。

概率输出区分：未校准排序分数、抽样权重、单号码入选边际概率、整注联合概率。SSQ 主区合法边际概率有 33 个值，每个在 [0,1] 且和为 6；蓝区和为 1。由各号独立拟合产生的输出须明确其估计含义与一致性处理，不假称联合分布。

主要指标：逐期命中数、Precision@K、Recall@K、明确事件定义的 Hit Rate、按号码平均的二元 Log Loss/Brier、蓝球类别指标及校准。比较同期开奖、相同预算、相同测试区间下的差值，保留逐期记录和适用的不确定性。排名分数不可直接代入概率损失。

实验保存 run_id、全部参数、随机种子、数据与代码版本、时间切分、模型和特征版本、指标及诊断。判定可用 NOT_SIGNIFICANT、WEAK_SIGNAL、SIGNIFICANT、POSSIBLE_LEAKAGE、INVALID_EXPERIMENT；判定依据必须可审计。

## Monte Carlo、ROI 与覆盖

支持不同模拟次数与资源上限，按完整合法开奖进行不放回抽样，记录 seed、迭代数、标准误或置信区间。Monte Carlo p 值采用合适的有限样本计算，不能因零次超越而直接输出确定的 p=0。

ROI 为 `(总派奖−总成本)/总成本`，按对应期的规则与可追溯历史派奖核算；缺失浮动奖、票价或税费口径时明确哪些结果不可计算。情景模拟与历史实现收益分开，不将全行业返奖率当作每注固定收益。

覆盖优化接收注数/预算、候选集合、目标命中阈值及约束，输出合法去重组合、成本、覆盖分母、重复度和方法。首版使用贪心；小空间精确枚举验证，大空间估计附区间。局部搜索与其他优化器属于后续方法扩展。候选子集的条件覆盖须注明成立前提，启发式结果不得宣称已证明全局最优。

## API 与界面

API 统一使用 `/api/v1`，至少覆盖 health、draws（列表/最新/期号）、statistics（frequency/omission/randomness）、models、backtests（提交/状态/结果）、simulations、strategies/generate、optimizations/covering。

所有请求有类型、范围及资源限制，错误响应可解释，分页明确总数/游标。本地长任务提交后返回 job_id，云端提交等待限时任务结束；两者均保留 ID、状态、失败原因和结果。云端读取、导出与写入均需管理令牌，仅健康检查/规则/模型目录公开；日志不记录凭据。云端上传上限为 4 MiB。

页面包括总览、历史开奖、统计、模型实验、回测、Monte Carlo、覆盖实验和方法说明；中文为默认语言，响应式布局。展示来源、时间范围、样本量、数据是否合成及实验不确定性。模型榜单展示完整比较和筛选条件，不仅展示胜者。

## 验收契约

1. 数据可重复导入；非法和冲突记录被隔离并可解释；真实完整性报告有可核对的分母。
2. 统计与组合公式通过小样本/精确结果对照；抽样与不确定性计算正确。
3. 未来数据扰动不改变过去特征/预测；训练侧拟合范围被实际检查。
4. 基线、至少一个线性模型和一个树模型使用相同的时间回测协议，保存结果并可复现。
5. 页面真实连接 API，关键流程、加载/空值/错误/窄屏通过实际验证。
6. 全新环境可按 README 安装、迁移、启动、导入和测试；Docker 重启保留数据。
7. 适用的 lint、类型检查、测试与构建通过，线上 CI 的状态按真实运行报告。
8. 科学审计与最终验收列出证据、未执行项和已知限制；无未处理的阻断问题才称平台完成。
9. 云版在真实 Vercel/外部 PostgreSQL 上通过鉴权、查询、计算、CSV、持久化及来源连通性检查；本机关闭后仍可使用。发布前迁移不得覆盖非空目标，逐表指纹、原始快照与历史任务必须完整保留。
